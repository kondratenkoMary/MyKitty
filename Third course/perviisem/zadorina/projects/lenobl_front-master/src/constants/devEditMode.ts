/**
 * @abstract Режим редактирования в разделе Разработка
 * @enum {number}
 */
export enum DevEditMode {
    /* Паспорт */
    PASSPORT = 0,
    /* Планы */
    PLANS = 1,
    /* Факты */
    FACTS = 2
}